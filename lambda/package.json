{
  "name": "leave-now-commute-oracle",
  "version": "1.0.0",
  "description": "Leave Now London Commute Oracle — Alexa skill backend",
  "main": "index.js",
  "engines": {
    "node": ">=16.x"
  },
  "scripts": {
    "test": "jest --runInBand",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "ask-sdk-core": "^2.14.0",
    "ask-sdk-model": "^1.29.0",
    "ask-sdk-dynamodb-persistence-adapter": "^2.14.0",
    "axios": "^1.18.1"
  },
  "devDependencies": {
    "axios-mock-adapter": "^2.1.0",
    "jest": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/test/**/*.test.js"]
  }
}
