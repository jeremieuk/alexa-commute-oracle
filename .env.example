# Local development only — never commit .env to source control
# In Alexa-hosted production, TFL_APP_KEY is set in lambda/config.js directly
# (Alexa-hosted provides no custom env-var UI)
TFL_APP_KEY=your_tfl_app_key_here
